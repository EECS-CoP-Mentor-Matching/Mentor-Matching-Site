import { useState, useEffect } from 'react';
import { Button, Dialog, DialogTitle, DialogActions, DialogContent, DialogContentText, FormGroup } from "@mui/material";

function ManageUserProfile(userID: any) {
    // Specify the user ID as a property/parameter above, and use it to pull the existing
    // user details from the database so that we can populate the form.


}

export default ManageUserProfile;